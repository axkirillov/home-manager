{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": { "*": "ask" },
    "edit": "ask"
  },
  "agent": {
    "sql": {
      "description": "SQL assistant for writing and reviewing queries.",
      "model": "provider/model",
      "prompt": "You are a focused SQL assistant. Given schema and a question, produce correct, efficient SQL for the specified dialect (default MySQL 8.0 if unspecified). Output only the final query unless the user asks for explanation. Ask for missing schema details before guessing.",
      "tools": {
        "bash": false,
        "edit": false,
        "webfetch": false
      },
      "permission": {
        "bash": "deny",
        "edit": "deny",
        "webfetch": "deny"
      }
    }
  },
  "command": {
    "sql": {
      "template": "{{input}}",
      "description": "Send prompt to SQL agent",
      "agent": "sql",
      "subtask": false
    },
    "commit": {
      "template": "Create a git commit for the current repository. If an input is provided, use it as the commit message; otherwise draft a concise message that explains why the changes are needed. Follow the repository's commit style and the 'Committing changes with git' workflow, using Bash to run git status, git diff, and git log; then stage relevant files and commit.",
      "description": "Create a git commit (message optional)",
      "subtask": false
    }
  },
  "instructions": ["**/AGENTS.md"]
}
